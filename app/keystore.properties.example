# Copy this file to keystore.properties and fill in real values.
# keystore.properties is gitignored and never committed.
#
# storeFile is relative to the app/ module directory.
storeFile=release.keystore
storePassword=REPLACE_WITH_STRONG_PASSWORD
keyAlias=silentguardian
keyPassword=REPLACE_WITH_STRONG_PASSWORD
